private void maximumSum(int[] arr){
		int max = arr[0], sum = arr[0];
		for(int i  = 1; i < arr.length; i++){
			sum += arr[i];
			if(sum > max){
				max = sum;
			}
		}
		System.out.println("Maximum Continguous Sum = "+max);
	}
